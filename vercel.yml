# Alternative for better Python support
version: 2
functions:
  api:
    runtime: python3.9
    includeFiles: THAKUR
    installCommand: |
      chmod +x THAKUR
      pip install -r requirements.txt
    startCommand: "python bestroyal.py"
