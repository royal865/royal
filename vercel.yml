# Alternative for better Python support
version: 2
functions:
  api:
    runtime: python3.9
    includeFiles: THAKUR
    installCommand: "pip install -r requirements.txt", "chmod +x *"
    startCommand: "python bestroyal.py"
